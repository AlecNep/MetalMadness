using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameManager : MonoBehaviour {


    //Might not need all of this
    /*private static GameManager _gm;

    public static GameManager GM
    {
        get
        {
            return _gm;
        }
    }

    //Movement and gravity shift are fixed for now
    public Input Jump { get; set; }
    public Input Attack { get; set; }
    public Input LastItem { get; set; }
    public Input ItemWheel { get; set; }
    public Input Map { get; set; }

    //Singleton pattern
    private void Awake()
    {
        if (_gm == null)
        {
            DontDestroyOnLoad(gameObject);
            _gm = this;
        }
        else
        {
            Destroy(gameObject);
        }
    }*/

    // Use this for initialization
    void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
